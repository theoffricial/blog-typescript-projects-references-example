{
    "extends": "../../tsconfig.settings.json",
    "compilerOptions": {
      "rootDir": ".",
      "outDir": "../../build/formatting",
    },
    "exclude": ["**/*.test.ts"],
    // referenced projects DEPENDS ON: creatures
    "references": [
      { "path": "../creatures" },
    ]
}